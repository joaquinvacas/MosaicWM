<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.mosaic-wm" path="/org/gnome/shell/extensions/mosaic-wm/">
    <key name="tile-left" type="as">
      <default>['&lt;Super&gt;Left']</default>
      <summary>Tile window to left</summary>
      <description>Tile the active window to the left half of the screen</description>
    </key>
    <key name="tile-right" type="as">
      <default>['&lt;Super&gt;Right']</default>
      <summary>Tile window to right</summary>
      <description>Tile the active window to the right half of the screen</description>
    </key>
    <key name="maximize-window" type="as">
      <default>['&lt;Super&gt;Up']</default>
      <summary>Maximize window</summary>
      <description>Maximize the active window, or move it up the tiling stack</description>
    </key>
    <key name="restore-window" type="as">
      <default>['&lt;Super&gt;Down']</default>
      <summary>Restore window</summary>
      <description>Move the active window down the tiling stack, back to the mosaic</description>
    </key>
    <key name="swap-left" type="as">
      <default>['&lt;Super&gt;&lt;Alt&gt;Left']</default>
      <summary>Swap window to the left</summary>
      <description>Swap the active window with its left neighbor (mosaic or tiling)</description>
    </key>
    <key name="swap-right" type="as">
      <default>['&lt;Super&gt;&lt;Alt&gt;Right']</default>
      <summary>Swap window to the right</summary>
      <description>Swap the active window with its right neighbor (mosaic or tiling)</description>
    </key>
    <key name="swap-up" type="as">
      <default>['&lt;Super&gt;&lt;Alt&gt;Up']</default>
      <summary>Swap window upward</summary>
      <description>Swap the active window with its upper neighbor (quarter tiles only)</description>
    </key>
    <key name="swap-down" type="as">
      <default>['&lt;Super&gt;&lt;Alt&gt;Down']</default>
      <summary>Swap window downward</summary>
      <description>Swap the active window with its lower neighbor (quarter tiles only)</description>
    </key>
    <key name="float-toggle" type="as">
      <default>['&lt;Super&gt;F']</default>
      <summary>Float or unfloat the active window</summary>
      <description>Toggle the active window between floating (always-on-top, outside the mosaic) and tiled back into the mosaic</description>
    </key>
  </schema>
</schemalist>
